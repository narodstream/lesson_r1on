import Button from './Button';

const Description = (props) => {
    if (jfioqejfieqifq) {
        props.getMoney();
    }
    const dataFromServer = '...';

    return (
        <Button dataFromServer={dataFromServer}></Button>
    );
};

export default Description;